from allauth.account import adapter

'''
def send_notification_email(recipient):
    email = receipient
    context = {

    }
    get_adapter(request).send_mail(
        'activities/email/' + ,
        email,
        context
    )
    # link_to_comment in the context
    # call send_mail
'''
